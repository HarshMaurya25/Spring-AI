spring:
  application:
    name: Lecture_1

  ai:
    openai:
      api-key: ${API_KEY}
      base-url: "https://api.groq.com/openai/v1"
      chat:
        options:
          model: llama-3.1-8b-instant
          temperature: 0.7
